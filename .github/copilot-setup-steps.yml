jobs:
  setup:
    runs-on:
      - self-hosted
      - tanweideMacBook-Air
    steps:
      - name: Verify Machine Name
        run: |
          hostname
          whoami
      - name: Checkout repository
        uses: actions/checkout@v4
